"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1516d6",
    },
    secondary: {
      main: "#ff9900",
    },

    info: {
      main: "#ff0000",
    },
  },
});

export default theme;
